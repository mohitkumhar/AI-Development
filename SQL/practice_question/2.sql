-- 2. Filtering Rows
--    
--    Write a SQL query to find all the employees in the `employees` table whose `salary` is greater than 50000.

USE practice;
SELECT * FROM employees WHERE salary > 50000;
